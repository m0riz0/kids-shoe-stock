# PROJECT_BRIEF.md

# プロジェクト概要

## プロジェクト名

仮称：Kids Shoe Stock

## 概要

子供2人分の靴を管理するための、家族向けスマホWebアプリ。

子供は成長が早く、将来使うサイズの靴を先に購入してストックしておくことがある。しかし、購入したサイズを忘れたり、どこに保管したか分からなくなったりすることで、以下の問題が発生している。

- すでに持っているサイズの靴を重複購入してしまう
- 必要なサイズの靴を持っているかすぐに確認できない
- ストックした靴をどこに保管したか分からなくなる
- 「家（登園）」「園庭」「上履き」など用途ごとの在庫状況を把握しづらい

このサービスは、子供ごとの靴のサイズ・カテゴリ・状態・保管場所を一元管理し、必要な靴をすぐに確認できるようにすることを目的とする。

---

# プロダクトの思想

「今、どの靴を持っているか」と「その靴がどこにあるか」を、スマートフォンからすぐに確認できることを最優先とする。

特に、靴を購入する際に、

> 「このサイズの靴、もう持ってたっけ？」

を数秒で確認できる体験を重視する。

機能を増やしすぎず、まずは家庭内で実際に使える最小限のプロダクトとして開発する。

現時点では対象を「子供の靴」に限定する。

将来的な拡張性は考慮してもよいが、MVPでは衣類など他カテゴリの商品管理機能は実装しない。

---

# MVPの対象ユーザー

- 子供を持つ家庭
- 子供の靴を先買いしてストックする家庭
- 夫婦など複数人で子供の靴を管理したい家庭

MVPでは、まず開発者本人と配偶者の家庭内利用を主なユースケースとする。

ただし、アプリ自体は一般公開可能な構成とし、将来的に他の家庭も利用できる設計とする。

ユーザーごとにデータを分離し、他の家族の靴データを閲覧できないようにする。

---

# MVP機能

## 1. ユーザー認証

夫婦2人がそれぞれのスマートフォンから同じ家族のデータを利用できるようにする。

MVPではSupabase Authを利用する。

認証方式はGoogleログインを基本とする。

### 認証と家族データの関係

Googleアカウントはユーザー本人を識別するために利用する。

例えば、

- 夫のGoogleアカウント
- 妻のGoogleアカウント

はそれぞれ別のユーザーアカウントとして管理する。

その上で、2人のユーザーアカウントを同じFamilyに所属させることで、同じ家族の靴データを共有する。

概念的には以下の構造とする。

```text
Family
「我が家」
│
├── User: 夫のGoogleアカウント
├── User: 妻のGoogleアカウント
│
├── Child: 長男
│    ├── Shoe
│    └── Shoe
│
└── Child: 次男
     ├── Shoe
     └── Shoe
```

夫が登録した靴は妻も閲覧・編集でき、妻が登録した靴は夫も閲覧・編集できる。

---

## 2. 家族単位のデータ共有

ユーザーはFamilyに所属する。

Family単位で子供と靴のデータを管理する。

ユーザーが複数存在する場合でも、同じFamilyに所属していれば同じ靴データを共有できる。

MVPでは、開発者本人と配偶者の2ユーザーでの利用を想定する。

将来的に家族メンバーを追加できる設計にしてもよい。

---

## 3. 子供の切り替え

子供ごとに靴を管理できる。

MVPでは2人の子供を想定する。

画面上で以下のように子供を切り替えられる。

- 長男
- 次男

将来的に子供を追加できる設計にしてもよいが、MVPでは固定でもよい。

---

## 4. 靴一覧

選択した子供の靴を、サイズ順に一覧表示する。

サイズごとに以下の情報を確認できる。

- サイズ
- カテゴリ
- 状態
- ブランドなどの基本情報
- 保管場所

特に「どのサイズの靴を持っているか」が一目で分かるUIを重視する。

同じサイズでもカテゴリごとに靴の有無・状態を確認できるようにする。

例：

```text
18cm

家（登園）  → ストック
園庭        → ストック
上履き      → なし
```

---

## 5. 靴カテゴリ

靴には以下のカテゴリを設定する。

- 家（登園）
- 園庭
- 上履き

カテゴリは固定値とする。

同じサイズでもカテゴリが異なる靴を別々に管理できるようにする。

---

## 6. 靴の登録

以下の情報を登録できる。

### 必須

- 子供
- サイズ
- カテゴリ
- 状態

### 任意

- ブランド
- 靴の写真
- 保管場所
- 保管場所のメモ

---

## 7. 靴の状態

靴には以下の状態を設定する。

- 使用中
- ストック
- サイズアウト

状態によって一覧上で区別できるようにする。

---

## 8. 保管場所

ストックしている靴の保管場所を登録できる。

例：

- 押入れ上段
- 玄関収納
- 子供部屋クローゼット

保管場所は自由入力とする。

---

## 9. 靴の写真

靴の写真を1枚以上登録できる。

写真はスマートフォンから撮影または選択できるようにする。

Supabase Storageへの保存を想定する。

---

## 10. 保管場所メモ

保管場所について自由にメモを登録できる。

例：

- 「押入れ上段の右側」
- 「青い収納ボックス」
- 「冬物の服の奥」

---

## 11. 靴の編集・削除

登録済みの靴情報を編集・削除できる。

状態変更も簡単に行えるようにする。

特に、

- ストック → 使用中
- 使用中 → サイズアウト

の変更を簡単に行えるUIを重視する。

---

# 主要ユースケース

## ユースケース1：靴を買う前に確認する

1. スマートフォンでアプリを開く
2. 子供を選択
3. サイズ一覧を見る
4. 購入予定のサイズを確認
5. そのサイズ・カテゴリの靴がすでにストックされているか確認する
6. 持っていれば重複購入を避ける

---

## ユースケース2：ストックしていた靴を探す

1. 子供を選択
2. サイズを確認
3. カテゴリを確認
4. 靴の詳細を開く
5. 保管場所を確認
6. 必要に応じて写真や保管場所メモを見る

---

## ユースケース3：新しい靴をストックする

1. 靴を購入する
2. アプリで靴を登録する
3. 子供を選択
4. サイズを入力
5. カテゴリを選択
6. 状態を「ストック」にする
7. 保管場所を入力
8. 写真を登録する

---

# UI / UX方針

- スマートフォンでの利用を最優先
- 片手操作しやすいUI
- できるだけ入力項目を減らす
- 靴の登録を短時間で完了できるようにする
- サイズごとの在庫状況を直感的に確認できる
- 「買う前の確認」が最短操作でできる
- 保管場所をすぐ確認できる
- 使用中・ストック・サイズアウトを視覚的に区別する
- PWAとしてスマートフォンのホーム画面から起動できるようにする

---

# 技術スタック

## フロントエンド

- Next.js
- TypeScript
- Tailwind CSS

## バックエンド / データベース

- Supabase
- PostgreSQL

Supabaseは、データベース・認証・ファイルストレージなど、Webアプリのバックエンド機能をまとめて提供するBaaSとして利用する。

今回のアプリでは、以下の用途で利用する。

- PostgreSQL：家族・子供・靴データの保存
- Supabase Auth：ユーザー認証
- Supabase Storage：靴の写真の保存
- Supabase RLS：家族単位のデータアクセス制御

## 認証

- Supabase Auth
- Google OAuth

ユーザーごとにGoogleアカウントを利用してログインする。

夫婦それぞれが異なるGoogleアカウントを使用し、それぞれ別のUserとして認識する。

その上で、Familyへの所属関係によって同じ家族のデータを共有する。

## 画像ストレージ

- Supabase Storage

靴の写真を保存する。

画像データについても、家族単位でアクセス制御を行う。

## ホスティング

- Vercel

## モバイル対応

- PWA

---

# 想定アーキテクチャ

```text
スマートフォン
    ↓
PWA / Next.js
    ↓
Supabase
    ├── Supabase Auth
    │      └── Google OAuth
    │
    ├── PostgreSQL
    │      ├── User
    │      ├── Family
    │      ├── FamilyMember
    │      ├── Child
    │      └── Shoe
    │
    └── Supabase Storage
           └── 靴の写真
```

---

# セキュリティ / データ分離要件

このサービスは一般公開可能なWebアプリとして構築する。

そのため、開発者本人と配偶者以外のユーザーが利用する可能性を考慮し、家族単位でデータを完全に分離する。

## 基本方針

- ユーザーはGoogleアカウントで認証する
- ユーザーは1つ以上のFamilyに所属する
- ChildはFamilyに所属する
- ShoeはChildに所属する
- 認証済みユーザーは、自分が所属するFamilyのデータのみ閲覧・作成・更新・削除できる
- 他のFamilyに所属するユーザーのデータは閲覧できない
- クライアント側のUIによる制御だけに依存しない

## Supabase RLS

データベースにはSupabaseのRow Level Security（RLS）を利用する。

RLSにより、認証済みユーザーが所属するFamilyのデータのみCRUDできるようにする。

例えば、

```text
Family A
├── User A1
├── User A2
└── Child / Shoe data

Family B
├── User B1
└── Child / Shoe data
```

の場合、

- User A1 → Family Aのデータのみアクセス可能
- User A2 → Family Aのデータのみアクセス可能
- User B1 → Family Bのデータのみアクセス可能

とする。

User A1がFamily Bのデータを直接API経由で取得しようとしても、RLSによって拒否される設計とする。

## Storageのアクセス制御

靴の写真についても、データベースと同様に家族単位でアクセス制御する。

写真ファイルを単純に全体公開する設定にはしない。

Supabase Storageのアクセス制御を適切に設定し、認証済みユーザーが自分の所属するFamilyに紐づく写真のみアクセスできるようにする。

DBのRLSだけでなく、Storage側のアクセス制御も必ず実装する。

## セキュリティ上の重要事項

- 家族単位のデータ分離を必須とする
- RLSを必ず設定する
- クライアント側でのアクセス制御だけに依存しない
- Storageの画像も家族単位でアクセス制御する
- SupabaseのService Role Keyなどの秘密情報をクライアントに露出させない
- 公開環境でも他のFamilyのデータにアクセスできないことを確認する
- 認証・認可のテストを実装する

---

# データモデルの基本方針

## User

- id
- email
- created_at

Supabase Authのユーザーと紐づける。

## Family

- id
- name
- created_at

## FamilyMember

- family_id
- user_id
- created_at

UserとFamilyの所属関係を管理する。

## Child

- id
- family_id
- name
- created_at

## Shoe

- id
- child_id
- category
- size
- status
- brand
- storage_location
- storage_note
- image_url
- created_at
- updated_at

ChildからFamilyを特定できる構造とし、ユーザーが所属するFamilyのデータのみアクセスできるようにする。

必要に応じて、Shoeにfamily_idを直接保持する設計も検討してよい。

---

# MVPで実装しないもの

以下はMVPでは実装しない。

- 衣類管理
- おもちゃ管理
- 靴以外の商品管理
- AI機能
- サイズの自動推定
- 足のサイズ記録
- 成長予測
- 通知
- 購入履歴
- ECサイト連携
- バーコード・QRコード
- SNS機能
- 管理画面
- 複雑な権限管理
- 家族以外とのデータ共有

---

# 開発方針

まずMVPを最短で完成させる。

過剰な抽象化や将来の拡張を目的とした複雑な設計は避ける。

ただし、以下は将来的な拡張を妨げないようにする。

- 子供の追加
- カテゴリの追加
- 家族メンバーの追加

MVPでは「実際に妻がスマートフォンで使えること」を最優先する。

同時に、一般公開した場合でもユーザー間でデータが混ざらない、安全なマルチテナント構成を採用する。

開発開始前に、現在のプロジェクト構成と使用可能な環境を確認する。

実装前に、以下を整理する。

1. 画面構成
2. Supabaseのテーブル設計
3. Google OAuthの設定方法
4. FamilyとUserの紐付け方法
5. RLS（Row Level Security）ポリシー
6. Storageの構成
7. Storageのアクセス制御
8. PWA設定
9. ローカル開発環境
10. Vercelへのデプロイ方法
11. 認証・認可のテスト方法

設計が完了したらMVP実装を開始する。